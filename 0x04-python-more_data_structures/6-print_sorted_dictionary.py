#!/usr/bin/python3
def print_sorted_dictionary(a_dictionary):
    sorted_keys = sorted(a_dictionary.keys())

    for key in new_keys:
        value = a_directory[keys]
        print(f"{key}: {value}")
